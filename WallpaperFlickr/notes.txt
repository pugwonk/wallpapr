Before build:

* Remember to build Release (both Setup and WallpaperFlickr)
* Remember to increment BOTH ASSEMBLY AND FILE version on any assemblies that have changed (probably just the one)
* Remember to increment setup version