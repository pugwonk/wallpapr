<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:Avalon.Windows.Controls" x:Class="WallPapr.MainWindow"
        Title="WallPapr" Height="350" Width="525" Icon="Flickr.png" WindowState="Minimized"   >
    <Grid >
        <TextBlock 
            Text="No settings yet, lets work on replicating  the functionality in the WInForms client before committing to WPF, as it looks as if everything runs from the main form." HorizontalAlignment="Stretch" Margin="10,10,10,0" TextWrapping ="Wrap"
               VerticalAlignment="Top"/>

        <Controls:NotifyIcon x:Name="notifyIcon"
            HorizontalAlignment="Left"  VerticalAlignment="Top" Icon="Flickr.png" Click="_notifyIcon_Click" >
            <Controls:NotifyIcon.ContextMenu>
                <ContextMenu >
                    <MenuItem Header="Settings..." 
                              Click="MenuItem_ClickSettings" />
                    <MenuItem Header="Get new wallpaper" FontWeight="Bold" Click="MenuItem_ClickGetNewPaper"
                             />
                    <MenuItem Header="This photo on flickr.com..."/>
                    <MenuItem Header="Exit"/>
                </ContextMenu>
            </Controls:NotifyIcon.ContextMenu>
        </Controls:NotifyIcon>

    </Grid>
</Window>
